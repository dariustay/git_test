CREATE OR REPLACE PROCEDURE PSA_TEST.PUBLIC.GET_EMPLOYEES_START_WITH_3()
RETURNS TABLE()
LANGUAGE SQL
AS
$$
BEGIN
    LET results RESULTSET := (
        SELECT *
        FROM PSA_TEST.PUBLIC.EMPLOYEE1
        WHERE ID::TEXT LIKE '3%'
        );

    RETURN TABLE(results);
END
$$;
