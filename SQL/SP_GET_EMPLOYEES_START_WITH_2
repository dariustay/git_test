CREATE OR REPLACE PROCEDURE PSA_TEST.PUBLIC.GET_EMPLOYEES_START_WITH_2()
RETURNS TABLE()
LANGUAGE SQL
AS
$$
BEGIN
    LET results RESULTSET := (
        SELECT *
        FROM PSA_TEST.PUBLIC.EMPLOYEE1
        WHERE ID::TEXT LIKE '2%'
        );

    RETURN TABLE(results);
END
$$;